{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capability for all three Talkie windows. Window visibility is driven from Rust (tray, shortcut, close-to-hide), so the webview needs no window permissions of its own.",
  "windows": [
    "editor",
    "settings",
    "onboarding"
  ],
  "permissions": [
    "core:default",
    "global-shortcut:default"
  ]
}
